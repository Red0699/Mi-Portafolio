---
import { EXPERIENCIE } from "@/data/experiencia";
import { PROJECTS } from "@/data/projects";
import { CERTIFICACIONES } from "@/data/certificaciones";
import { SKILLS } from "@/data/skills"; // si decides tenerlas separadas
---

<html lang="es">
  <head>
    <meta charset="UTF-8" />
    <title>Currículum de Nicolás Campos</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <style>
      @media print {
        .no-print {
          display: none;
        }
        html {
          zoom: 0.95;
        }
      }
    </style>
  </head>
  <body class="bg-white text-gray-900 font-sans px-6 py-12 max-w-3xl mx-auto print:px-0 print:py-4">

    <header class="border-b pb-4 mb-6">
      <h1 class="text-3xl font-bold">Nicolás Campos</h1>
      <p class="text-lg text-gray-600">Ingeniero de Sistemas | Desarrollador Web Full Stack</p>
      <p class="text-sm text-gray-500 mt-2">📍 Bogotá, Colombia · 📧 red0699@gmail.com · 💻 github.com/Red0699</p>
    </header>

    <section class="mb-6">
      <h2 class="text-xl font-semibold border-b pb-1 mb-2">Perfil</h2>
      <p class="text-sm leading-relaxed text-gray-700">
        Ingeniero de Sistemas con experiencia en desarrollo web Full Stack usando tecnologías como Laravel, Node.js, Angular, MySQL y más. Apasionado por crear soluciones eficientes, con enfoque en usabilidad, rendimiento y buenas prácticas.
      </p>
    </section>

    <section class="mb-6">
      <h2 class="text-xl font-semibold border-b pb-1 mb-2">Experiencia Laboral</h2>
      <ul class="space-y-4 text-sm">
        {EXPERIENCIE.map(({ title, company, date, description }) => (
          <li>
            <strong>{title}</strong> · <span class="italic">{company}</span>  
            <p class="text-gray-500">{date}</p>
            <p class="text-gray-700 mt-1">{description}</p>
          </li>
        ))}
      </ul>
    </section>

    <section class="mb-6">
      <h2 class="text-xl font-semibold border-b pb-1 mb-2">Proyectos Destacados</h2>
      <ul class="space-y-4 text-sm">
        {PROJECTS.slice(0, 3).map(({ title, description, github }) => (
          <li>
            <strong>{title}</strong>
            <p class="text-gray-700">{description}</p>
            {github && (
              <a href={github} class="text-blue-600 hover:underline text-xs" target="_blank">Repositorio</a>
            )}
          </li>
        ))}
      </ul>
    </section>

    <section class="mb-6">
      <h2 class="text-xl font-semibold border-b pb-1 mb-2">Certificaciones</h2>
      <ul class="text-sm space-y-2">
        {CERTIFICACIONES.map(cert => (
          <li>
            <strong>{cert.titulo}</strong> – {cert.emisor}, {cert.expedicion}  
            <a href={cert.link} target="_blank" class="text-blue-600 hover:underline text-xs ml-1">Ver</a>
          </li>
        ))}
      </ul>
    </section>

    <section class="mb-6">
      <h2 class="text-xl font-semibold border-b pb-1 mb-2">Habilidades Técnicas</h2>
      <ul class="flex flex-wrap gap-2 text-sm">
        {PROJECTS.flatMap(p => p.tags.map(tag => tag.name))
          .filter((value, index, self) => self.indexOf(value) === index)
          .map(skill => (
            <li class="bg-gray-200 text-gray-800 px-2 py-1 rounded">{skill}</li>
          ))}
      </ul>
    </section>

    <footer class="text-center text-sm text-gray-500 mt-10 no-print">
      <button onclick="window.print()" class="text-blue-600 hover:underline">🖨️ Imprimir / Descargar en PDF</button>
    </footer>

  </body>
</html>
